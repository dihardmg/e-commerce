# RabbitMQ configuration for E-Commerce platform

# Default user and vhost
default_user = admin
default_pass = password
default_vhost = /

# Networking
listeners.tcp.default = 5672

# Disk and memory thresholds
disk_free_limit.absolute = 1GB
vm_memory_high_watermark.relative = 0.6

# Enable management plugin
management.tcp.port = 15672
management.tcp.ip = 0.0.0.0

# Heartbeat settings
heartbeat = 60

# Consumer timeout
consumer_timeout = 1800000

# Queue master locator
queue_master_locator = min-masters

# Enable metrics and monitoring
collect_statistics_interval = 10000

# clustering configuration (if needed in the future)
# cluster_formation.peer_discovery_backend = classic_config
# cluster_formation.classic_config.nodes.1 = rabbit@ecommerce-rabbitmq